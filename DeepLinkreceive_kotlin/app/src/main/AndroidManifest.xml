<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.example.deeplink_receive">

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/Theme.DeepLinkreceive">

        <activity
            android:name=".MainActivity"
            android:exported="true">

            <!-- 시작 지점 -->
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>

            <!-- DeepLink -->
            <intent-filter>
                <action android:name="android.intent.action.VIEW" />
                <category android:name="android.intent.category.DEFAULT" />
                <category android:name="android.intent.category.BROWSABLE" />
                <data android:scheme="https" android:host="shopping.naver.com/home"/>
                <data android:scheme="http" android:host="shopping.naver.com/home"/>

<!--                <data android:scheme="https" android:host="deeplink2022.page.link/data"/>-->
<!--                <data android:scheme="http" android:host="deeplink2022.page.link/data"/>-->
<!--                https://deeplink2022.page.link/data/deeplinkdata-->

            </intent-filter>

        </activity>

    </application>

</manifest>